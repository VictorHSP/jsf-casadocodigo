<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>
        %d{yyyy-MM-dd HH:mm:ss} [%thread] %highlight(%-5level) %boldMagenta(%logger{20}) : %msg%n
      </pattern>
    </encoder>
  </appender>

  <root level="debug">
    <appender-ref ref="Console" />
  </root>

  <logger name="br.com.ebookofcode" level="debug">
    <appender-ref ref="Console" />
  </logger>

</configuration>
